contract VarStore {
    function() { throw; }

    event VarLog(
        address _from,
        bytes variantDesc,
    );

    function saveVar(bytes) {
            VarLog(msg.sender, _assembly, _chrom, _pos, _ref, _alt);
    }
}
